<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyKotlinApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_green</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_blue</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/main_green</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.MyKotlinApplication.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyKotlinApplication.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyKotlinApplication.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Tab" parent="Widget.MaterialComponents.TabLayout">
        <item name="tabIndicatorColor">?attr/colorSecondary</item>
        <item name="android:background">?attr/colorSecondary</item>
        <item name="tabIconTint">@drawable/tab_selector</item>
    </style>

    <style name="TitleGeneral" >
        <item name="android:textColor">@color/font_gray</item>
        <item name="android:textSize">@dimen/font_size_40</item>
        <item name="android:gravity">center</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TitleGeneral.text" >
        <item name="android:textSize">@dimen/font_size_30</item>
    </style>

    <style name="InputTextGeneral">
        <item name="android:background">@drawable/text_input_background</item>
        <item name="android:textColor">@color/font_gray</item>
        <item name="android:padding">5dp</item>
        <item name="android:gravity">top</item>
    </style>

    <style name="ButtonMain">
        <item name="backgroundTint">@color/main_green</item>
        <item name="cornerRadius">5dp</item>
        <item name="strokeColor">@color/main_blue</item>
        <item name="strokeWidth">3dp</item>
        <item name="android:padding">20dp</item>
        <item name="android:textSize">@dimen/font_size_30</item>
    </style>
</resources>